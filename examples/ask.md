# Ask

Review the changes related to this task.

1. Correctness risks?
2. Missing edge cases?
3. Anything that should block ship?

Reply in FINDINGS format if the companion was spawned with `--format findings` or `--persona critic` / `auditor`.
